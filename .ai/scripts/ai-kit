#!/usr/bin/env bash
# AI-Kit v2 CLI Wrapper
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$ROOT"
source .ai/scripts/python-command.sh
PYTHON_CMD="$(ai_kit_python_command)" || { echo "AI-Kit: Python runtime not found" >&2; exit 127; }
exec "$PYTHON_CMD" .ai/engine/ai_kit.py "$@"
