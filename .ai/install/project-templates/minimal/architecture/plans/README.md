# Architecture plans

Store execution blueprints here. A plan should name the affected bounded
contexts, versioned contract references, migration/rollback considerations, and
the workflow tasks that verify delivery. Plans are not lifecycle authority;
`.ai-work/state/*.json` remains the control-plane state.
