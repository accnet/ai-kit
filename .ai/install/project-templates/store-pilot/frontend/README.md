# Frontend pilot

`src/create-store.ts` consumes the generated SDK type from
`contracts/generated/sdk/`. Regenerate that artifact from the approved OpenAPI
contract rather than hand-editing a local copy of its DTO.
